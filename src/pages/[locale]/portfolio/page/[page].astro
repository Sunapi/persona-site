---
import PortfolioPage from '../../../../components/pages/PortfolioPage.astro';
import { portfolioPagePaths } from '../../../../data/portfolio';
import { prefixedLocales } from '../../../../i18n';

export function getStaticPaths() {
  return portfolioPagePaths(prefixedLocales);
}

const { locale, page } = Astro.props;
---

<PortfolioPage locale={locale} page={page} />
